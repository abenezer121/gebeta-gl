self.onmessage = function(e) {
    const { type, data } = e.data;
    switch(type) {
      case 'calculateBounds':
        break;
      case 'projectCoordinates':
        break;
      default:
        break;
    }
  };